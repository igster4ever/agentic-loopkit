"""
agentic_loopkit/dashboard/constants.py

Shared constants for dashboard routes. Centralised here so retention window,
pagination limits, and formatting helpers stay consistent across all routes.
"""

DEFAULT_HOURS = 72
DEFAULT_LIMIT = 100
MAX_LIMIT     = 1000
